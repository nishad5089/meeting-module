import { MemTranslatePipe } from './mem-translate-pipe.pipe';

describe('MemTranslatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MemTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
